# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: cart.spec.js >> @cart Verify Product Added To Cart
- Location: tests\cart.spec.js:17:1

# Error details

```
TypeError: CheckoutPage is not a constructor
```

# Test source

```ts
  1  | const { test } = require("@playwright/test");
  2  | 
  3  | const config = require("../config/config");
  4  | 
  5  | const LoginPage = require("../pages/LoginPage");
  6  | 
  7  | const InventoryPage = require("../pages/InventoryPage");
  8  | 
  9  | const CartPage = require("../pages/CartPage");
  10 | 
  11 | const CheckoutPage = require("../pages/CheckoutPage");
  12 | 
  13 | const Logger = require("../utils/Logger");
  14 | const ScreenshotUtil = require("../utils/ScreenshotUtil");
  15 | const ReportManager = require("../utils/ReportManager");
  16 | 
  17 | test("@cart Verify Product Added To Cart", async ({ page }, testInfo) => {
  18 |   const login = new LoginPage(page);
  19 | 
  20 |   const inventory = new InventoryPage(page);
  21 | 
  22 |   const cart = new CartPage(page);
> 23 |   const checkout = new CheckoutPage(page);
     |                    ^ TypeError: CheckoutPage is not a constructor
  24 |   // Open App
  25 |   await login.open(config.url);
  26 | 
  27 |   // Login
  28 |   await login.login(config.username, config.password);
  29 | 
  30 |   // Capture Product + Add To Cart
  31 |   const productName = await inventory.addFirstProduct();
  32 |   await ReportManager.log(testInfo, "Selected Product: " + productName);
  33 |   //   Logger.info(`Selected Product: ${productName}`);
  34 |   await ReportManager.attachScreenshot(page, testInfo, "Selected_Product");
  35 |   //   console.log("Selected Product:", productName);
  36 | 
  37 |   // Open Cart
  38 |   await inventory.openCart();
  39 | 
  40 |   // Verify Product
  41 |   await ReportManager.log(
  42 |     testInfo,
  43 |     "Verified Product: " + productName + " is in the cart",
  44 |   );
  45 |   //   Logger.info(`Verified Product: ${productName} is in the cart`);
  46 |   await ReportManager.attachScreenshot(page, testInfo, "Verified_Product");
  47 |   await cart.verifyProduct(productName);
  48 | 
  49 |   await checkout.clickCheckout();
  50 | 
  51 |   await ReportManager.log(testInfo, "Checkout Started");
  52 | 
  53 |   await checkout.enterCustomerDetails("Ravi", "Tester", "500081");
  54 | 
  55 |   await ReportManager.attachScreenshot(page, testInfo, "Customer_Details");
  56 | 
  57 |   await checkout.clickContinue();
  58 | 
  59 |   await ReportManager.log(testInfo, "Order Overview Displayed");
  60 | 
  61 |   await ReportManager.attachScreenshot(page, testInfo, "Order_Overview");
  62 | 
  63 |   await checkout.clickFinish();
  64 | 
  65 |   await checkout.verifyOrderSuccess();
  66 | 
  67 |   await ReportManager.log(testInfo, "Order Successfully Placed");
  68 | 
  69 |   await ReportManager.attachScreenshot(page, testInfo, "Order_Success");
  70 | });
  71 | 
```